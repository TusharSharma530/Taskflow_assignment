# Port the backend API listens on
PORT=3000

# Path (relative to the repository root) of the SQLite database file.
# Use ":memory:" for an in-memory database (dev/test only).
DATABASE_PATH=./database/taskflow.db