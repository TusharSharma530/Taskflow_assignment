{
  "name": "taskflow",
  "version": "1.0.0",
  "private": true,
  "description": "TaskFlow - a lightweight Trello-style task board",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend",
    "seed": "npm run seed --workspace backend",
    "test": "npm test --workspace backend",
    "test:watch": "npm run test:watch --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}